<!DOCTYPE html>
<html lang="<%= locale %>" dir="<%= dir %>" data-theme="<%= theme %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="csrf-token" content="<%= csrfToken %>">
  <meta name="description" content="<%= typeof description !== 'undefined' && description ? description : 'Whale (الحوت) is a trust-first marketplace for buying and selling in Palestine and Arab cities.' %>">
  <meta property="og:title" content="<%= typeof title !== 'undefined' && title ? title + ' — Whale | الحوت' : 'Whale | الحوت' %>">
  <meta property="og:description" content="<%= typeof description !== 'undefined' && description ? description : 'Whale (الحوت) is a trust-first marketplace for buying and selling in Palestine and Arab cities.' %>">
  <meta property="og:type" content="website">
  <meta property="og:url" content="<%= typeof canonicalUrl !== 'undefined' && canonicalUrl ? canonicalUrl : '/' %>">
  <meta property="og:image" content="<%= typeof ogImage !== 'undefined' && ogImage ? ogImage : '/favicon.ico' %>">
  <meta property="og:site_name" content="Whale | الحوت">
  <title><%= typeof title !== 'undefined' && title ? title + ' — ' : '' %>Whale | الحوت</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
