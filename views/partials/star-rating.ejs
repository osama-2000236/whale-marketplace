<% var rating = typeof r !== 'undefined' ? r : 0; %>
<span class="star-rating" aria-label="<%= rating %> stars">
  <% for (var i = 1; i <= 5; i++) { %>
  <span class="star <%= i <= rating ? 'filled' : '' %>">★</span>
  <% } %>
</span>
