<%- include('partials/head') %>
<%- include('partials/navbar') %>
<main class="main-content">
  <div class="container">
    <div class="empty-state">
      <div class="empty-state-icon">🐋</div>
      <h3><%= t('general.not_found') %></h3>
      <p><%= locale === 'ar' ? 'الصفحة التي تبحث عنها غير موجودة' : 'The page you are looking for does not exist' %></p>
      <a href="/" class="btn btn-primary"><%= t('general.back') %></a>
    </div>
  </div>
</main>
<%- include('partials/footer') %>
